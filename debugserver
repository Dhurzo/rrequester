require 'sinatra' 
require 'json'

get '/' do 
content_type :text
  return JSON.pretty_generate(request.env)
end

post '/' do 
content_type :text
  return JSON.pretty_generate(request.env)
end

put '/' do 
content_type :text
  return JSON.pretty_generate(request.env)
end
